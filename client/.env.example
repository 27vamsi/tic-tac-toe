# Copy to client/.env for local development (Vite loads at dev/build time).
# On Render, these are set on the static site service (see render.yaml).

VITE_NAKAMA_HOST=127.0.0.1
VITE_NAKAMA_PORT=7350
VITE_NAKAMA_SSL=false
VITE_NAKAMA_KEY=defaultkey
